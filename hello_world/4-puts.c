#include <stdio.h>

/**
 * main - Entry point of the program
 *
 * This program prints a specific string to the standard output,
 * demonstrating the use of the puts function with proper grammar.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}

